<div class="card h-100">
    <a href="{{ $detailUrl }}">
        <img src="{{ $imageUrl }}" class="cover-image" alt="{{ $title }}">
    </a>
    <div class="card-body text-center">
        <h5 class="card-title">{{ $title }}</h5>
        <a href="{{ $detailUrl }}" class="btn btn-primary mt-3">{{ $btnText }}</a>
    </div>
</div>
